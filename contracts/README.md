# Shared contracts

Files in this directory are the source of truth for cross-team integration. Use a stable path and a Git revision in every handoff.

`orders/order.contract.ts` is a deliberately small example. Replace it with your project’s real OpenAPI, protobuf, JSON Schema, generated TypeScript, or other contract format.
