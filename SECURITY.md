# Security policy

Do not report vulnerabilities through public issues. Send a minimal reproduction and impact summary to the repository security contact once it is configured.

Until a public contact is configured, do not deploy this project on an untrusted network. The v1 service uses personal access tokens and a server-side GitHub token; both require HTTPS, secret rotation, and restricted host access.
