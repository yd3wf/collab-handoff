# Generate separate values with: node .\scripts\create-token.mjs
POSTGRES_USER=collab
POSTGRES_DB=collab_handoff
POSTGRES_PASSWORD=replace-with-a-random-password
HUB_BOOTSTRAP_TOKEN=replace-with-a-random-bootstrap-token
COLLAB_HANDOFF_PORT=8787
# Required to validate and snapshot private GitHub contract files. Leave blank for public repositories.
GITHUB_TOKEN=
